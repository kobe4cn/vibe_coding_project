# ===========================================
# Ticket Backend - 环境变量配置模板
# 复制此文件为 .env 并修改配置
# ===========================================

# 数据库连接 (必填)
DATABASE_URL=postgres://user:password@localhost:5432/ticket_db

# 测试数据库连接 (测试时使用)
TEST_DATABASE_URL=postgres://user:password@localhost:5432/ticket_db_test

# 服务配置
HOST=0.0.0.0
PORT=3000

# 日志级别
# 可选值: error, warn, info, debug, trace
RUST_LOG=info,sqlx=warn,tower_http=debug

# 附件存储
UPLOAD_DIR=./uploads
# 最大文件大小 (字节), 默认 10MB
MAX_FILE_SIZE=10485760

# CORS 配置
# 允许的前端地址，多个用逗号分隔
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:80

